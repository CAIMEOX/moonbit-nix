on:
  push:
    branches:
      - main
  schedule:
    - cron: "00 * * * *"

jobs:
  update-hash:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - run: |
          npm install
          node update.js
          git config --global user.email "1853884864@qq.com"
          git config --global user.name "CAIMEOX"
          git add .
          git commit -m "version: update hashes"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GH_USER_TOKEN }}
